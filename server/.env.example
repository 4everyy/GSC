# 城市数据准备后端服务配置
# 复制为 server/.env 并填写真实值（该文件不应提交到版本库）

# 监听端口（默认 8082，仅 127.0.0.1）
GCS_ADMIN_PORT=8082

# 管理令牌（必须设置，否则所有管理操作被拒绝）
# 前端需在项目根 .env 配置相同的 VITE_ADMIN_TOKEN
GCS_ADMIN_TOKEN=change-me-to-a-long-random-string
