# ============================================================
# 环境变量示例 —— 复制为 .env.local 后按实际环境修改
# .env.local 已被 .gitignore 忽略，不会提交到仓库
# ============================================================

# --- 登录门控 ------------------------------------------------
# 默认跳过登录页直接进入首页（演示阶段，业务请求不携带 token 头）。
# 如需恢复登录验证（先展示 LoginPage，登录成功缓存 token 后进入首页）：
# VITE_SKIP_LOGIN=false

# --- WebSocket 连接（联调真实后端时配置）---------------------
# 方式一【推荐·开发联调】：不配置 VITE_WS_URL，前端默认连同源 /ws，
#   由 Vite dev server 代理转发，代理目标由下行变量控制：
# 2026-09-24 实测：后端 HTTP 与 WS 同端口 2222（8080 无监听 ECONNREFUSED）；
# /ws?token=<JWT> 握手实测 101 Switching Protocols，无 token 401。后端地址变更时在 .env.local 覆盖。
VITE_WS_PROXY_TARGET=ws://192.168.120.43:2222

# 方式二【直连】：配置完整 WebSocket 地址，前端绕过代理直连后端。
#   注意浏览器跨域限制——后端需允许该 Origin，否则握手会被拒绝：
# VITE_WS_URL=ws://192.168.120.43:8080

# 生产环境：通常由 nginx 反代 /ws 到后端（见部署文档），前端保持默认即可；
# 若直连，则在构建时注入：VITE_WS_URL=wss://your-domain/ws

# --- HTTP API 代理（联调真实后端时配置）---------------------
# 设备状态轮询等 HTTP 请求默认连同源 /api/*，由 Vite dev server 代理转发：
VITE_API_PROXY_TARGET=http://192.168.120.43:2222
# 生产环境：由 nginx 反代 /api 到后端，前端保持默认即可。

# --- 后端联调说明 -------------------------------------------
# HTTP mock 层已移除（数据全部走前端本地 mock + WS 实时消息）；
# WebSocket 登录后自动连接并订阅消息，始终启用。

# --- 登录接口 ------------------------------------------------
# 登录走 POST /api/v1/auth/login（JSON 请求体，账号密码按 LoginPage
# 输入框传入），成功响应 { token, expires_at }；token 缓存于
# localStorage 并以 `token` 请求头注入后续 HTTP/WS 请求。
# 默认跳过登录直接进入首页；设置 VITE_SKIP_LOGIN=false 恢复登录流程
# （跳过时不携带 token 头，需后端不强制鉴权）。
