# 离线地图下载方案（客户端缓存版）

## 1. 背景与目标

### 1.1 现状

当前离线地图采用**服务端方案**（`docs/离线部署指南.md`）：

```
浏览器 ──HTTP──▶ tileserver-gl (Docker, :8081) ──▶ data/suzhou.mbtiles
```

- 地图数据（mbtiles）由运维人员预装在 Docker 容器中；
- 浏览器每次渲染都从 tileserver-gl 实时拉取瓦片；
- **局限**：前端无法感知"是否已下载""下载进度"，无法在浏览器侧管理缓存，断网时若 tileserver 未启动则地图完全空白。

### 1.2 新需求

态势大屏需要在不依赖 tileserver-gl 的前提下，也能呈现地图。即在**浏览器侧**完成瓦片的下载、缓存、渲染：

| 维度 | 要求 |
|------|------|
| 视觉 | ① 无网络时地图依旧可呈现；② 下载过程有进度条；③ 下载失败/成功有弹框 |
| 交互 | 从"系统配置"按钮进入 → 弹窗含两个 Tab："下载离线地图" / "本地" |
| 状态边界 | 未下载且无网→灰显瓦片 + 提示；下载中→进度条；部分缺失→缺失块灰显、已下载块正常 |

### 1.3 目标

在**保留现有 tileserver-gl 架构**的基础上，新增一层**浏览器端瓦片缓存**：

- 首次联网时，用户通过 UI 主动触发"下载离线地图"，前端按选定区域/层级从 tileserver-gl 批量拉取瓦片并存入 IndexedDB；
- 后续无论联网与否，前端优先从本地 IndexedDB 读取瓦片渲染，tileserver-gl 退化为"首次数据源"而非"运行时依赖"；
- 断网且本地有缓存 → 地图正常显示；断网且本地无缓存 → 灰显瓦片 + 提示。

---

## 2. 总体架构

```
┌─────────────────────────── 浏览器（地面站前端） ───────────────────────────┐
│                                                                          │
│   MapLibreContainer                                                      │
│     └─ transformRequest(url) ──────────┐                                 │
│                                        ▼                                 │
│   ┌───────────────────────────────────────────────┐                      │
│   │          TileCacheInterceptor                 │  ← 新增              │
│   │  (拦截 /tiles/{z}/{x}/{y}.pbf 请求)           │                      │
│   └───────────────┬───────────────────┬───────────┘                      │
│                   │                   │                                  │
│          命中缓存 │                   │ 未命中                            │
│                   ▼                   ▼                                  │
│   ┌───────────────────────┐   ┌───────────────────────┐                  │
│   │ IndexedDB             │   │ fetch(tileserver-gl)  │                  │
│   │ tile-cache store      │   │ → 写回 IndexedDB      │                  │
│   │ {z}/{x}/{y} → blob    │   └───────────────────────┘                  │
│   └───────────────────────┘                                              │
│                                                                          │
│   OfflineMapDialog（系统配置 → 下载离线地图 / 本地）  ← 新增              │
│     ├─ 下载：按区域 bbox + zoom 范围批量预取                              │
│     └─ 本地：列出已缓存区域、占用空间、删除                              │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
                                   │ 首次联网下载
                                   ▼
                     tileserver-gl (Docker, :8081)
```

**关键设计点**：

1. **拦截点**：复用 MapLibre 的 `transformRequest`（`src/components/MapLibreContainer/MapLibreContainer.tsx`）。配合自定义协议 `gcs-cache://`（`src/features/offline-map/tileProtocol.ts`）实现"先查 IndexedDB 缓存，命中则零网络返回；**未命中一律灰显，绝不在线回源**"——严格离线，无任何 Esri / 在线兜底。
2. **缓存粒度**：按归一化同源代理路径 `{sourceId}/{z}/{x}/{y}` 作为主键，Value 存原始 Blob（vector pbf / raster png）。Style JSON 与 Glyphs pbf 同样缓存。
3. **严格离线（无懒加载）**：运行时不做"浏览即回源"的懒加载，也不依赖 `navigator.onLine` 决定是否在线抓取。所有瓦片必须由**预下载引擎**提前入库，否则按灰显处理。
   - **预下载**（唯一入库途径）：用户在"下载离线地图"Tab 选定区域 + zoom 范围，前端批量从**本地** tileserver-gl 拉取并入库，支持进度条与断点续传。
   - **运行时**：`gcs-cache://` 协议仅查 IndexedDB，命中返回、未命中灰显（栅格灰色占位 / 矢量抛错），零网络。

---

## 3. 视觉与交互设计

### 3.1 入口：系统配置按钮

当前 `HomePage` 右上角已有 `EngineSwitch`（引擎切换）。需新增**系统配置**按钮（齿轮图标），点击弹出 `OfflineMapDialog`。

```
HomePage
  └─ SystemConfigButton（齿轮，右上角，与 EngineSwitch 并列）
       └─ onClick → setOpen(true)
            └─ OfflineMapDialog open={open}
                 ├─ Tab: 下载离线地图
                 └─ Tab: 本地
```

> **实现提示**：`StatusHeader`（`src/components/StatusHeader`）是顶部状态栏，系统配置按钮可挂在 `StatusHeader` 右端，或直接作为 `HomePage` 浮层（与 `EngineSwitch` 同级）。后者更轻量，无需改动 `StatusHeader`。

### 3.2 弹窗：OfflineMapDialog

#### Tab 1：下载离线地图

```
┌─ 离线地图管理 ─────────────────────────────── × ┐
│ ┌────────────┐ ┌─────────────┐                  │
│ │下载离线地图│ │   本地      │                  │
│ └────────────┘ └─────────────┘                  │
│                                                  │
│  底图：  ● 矢量暗色(dark)  ○ 卫星影像(satellite) │
│                                                  │
│  区域：  [苏州预设 ▼]  或  框选地图区域          │
│          西经____ 东经____ 南纬____ 北纬____    │
│                                                  │
│  层级：  最小 [ 8 ] ▼   最大 [ 14 ] ▼           │
│          预估瓦片数：12,345  约需空间：~240MB   │
│                                                  │
│  ┌──────────────────────────────────────────┐   │
│  │ ████████████░░░░░░░░░░░  56%  下载中…    │   │  ← 进度条
│  └──────────────────────────────────────────┘   │
│                                                  │
│             [ 开始下载 ]   [ 取消 ]              │
└──────────────────────────────────────────────────┘
```

**交互流程**：

1. 选择底图（dark/satellite）、区域（预设/自定义 bbox）、zoom 范围；
2. 实时计算预估瓦片数与磁盘占用（公式见 §6.2），超阈值（如 2GB）给出二次确认；
3. 点击"开始下载"：
   - 进度条 0→100%，显示"下载中… X / Y 块，约 Z MB"；
   - 可"取消"，已下载块保留（断点续传）；
4. **下载成功** → 绿色弹框提示"离线瓦片下载完成（共 N 块）"；
5. **下载失败** → 红色弹框提示错误原因（网络中断 / 磁盘满 / 服务不可达），已下载块保留。

#### Tab 2：本地

```
┌─ 离线地图管理 ─────────────────────────────── × ┐
│ ┌─────────────┐ ┌─────────────┐                │
│ │下载离线地图 │ │   本地       │                │
│ └─────────────┘ └─────────────┘                │
│                                                  │
│  已缓存底图：                                    │
│  ┌──────────────────────────────────────────┐   │
│  │ 矢量暗色  苏州  z8-z14  12,345块  240MB  │   │
│  │                            [查看] [删除] │   │
│  ├──────────────────────────────────────────┤   │
│  │ 卫星影像  苏州  z10-z16  45,678块  1.2GB │   │
│  │                            [查看] [删除] │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  总占用：1.44 GB / 2.0 GB  [清空全部]            │
│                                                  │
│  离线可用性：  ✓ 全部区域已缓存                  │
│                                                  │
└──────────────────────────────────────────────────┘
```

- 列出每个已缓存区域（底图类型、区域名、zoom 范围、块数、占用空间）；
- 支持单条删除、清空全部；
- 显示总占用与配额（IndexedDB 配额，通过 `navigator.storage.estimate()` 获取）；
- "离线可用性"指示：基于缓存覆盖率判断（全部覆盖=绿、部分缺失=黄、无缓存=灰）。

### 3.3 地图区域状态边界

中央地图区域（`MapLibreContainer`）根据缓存/网络状态呈现三种态：

| 状态 | 触发条件 | 视觉表现 |
|------|----------|----------|
| **未下载** | IndexedDB 无该视图瓦片（**无论联网与否**，严格离线） | 灰显瓦片（灰色棋盘背景）+ 中央提示"离线瓦片未下载，请前往 系统配置 → 下载离线地图" |
| **下载中** | 预下载任务进行中 | 底部悬浮进度条（复用 §3.2 进度条组件），地图可正常浏览已缓存区域 |
| **部分缺失** | 缓存有部分瓦片但当前视图存在未缓存块 | 已下载块正常渲染，缺失块灰显（灰色占位），无弹窗打断 |

**实现要点**：
- "灰显瓦片"通过 `gcs-cache://` 自定义协议（`tileProtocol.ts`）：缓存未命中时，栅格瓦片返回固定灰色 PNG 占位、矢量瓦片抛错露出暗色背景——**严格离线、不读取 `navigator.onLine`、不回源任何 tileserver / 在线服务**；
- **不使用 `navigator.onLine`**：严格离线机制下无「在线/离线」概念，运行时一律按缓存命中 / 未命中处理（已废弃旧版「断网 + 零缓存 → 全屏提示」分支与 `status === 'offline'`）；
- "中央提示"复用现有 `maplibre-status--error` 覆盖层（`MapLibreContainer.tsx`）：style.json 加载失败且无本地缓存时提示运维检查 tileserver-gl；
- "下载中进度条"作为独立浮层组件 `DownloadProgressBar`，挂载在 `HomePage` 地图层之上。

---

## 4. 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 客户端存储 | **IndexedDB**（通过 `idb` 库封装） | 容量大（GB 级）、支持 Blob、异步非阻塞 |
| 瓦片拦截 | **MapLibre `transformRequest`** | 项目已用此机制（URL 重写），扩展为缓存拦截零改造 |
| 批量下载 | **fetch + 并发池**（p-limit 风格，并发 6-8） | 避免压垮 tileserver-gl；支持 AbortController 中断 |
| 进度通信 | **React Context + useReducer** | 下载状态全局共享（弹窗、进度条、地图态） |
| 配额查询 | `navigator.storage.estimate()` | 标准 Web API，展示已用/剩余空间 |
| 持久化 | `navigator.storage.persist()` | 请求持久化存储，防浏览器自动清理 |

> **不引入 Service Worker**：SW 会拦截所有同源请求，调试复杂，且现有 `transformRequest` 已足够精细控制瓦片请求。SW 方案作为 §8 备选。

---

## 5. 模块拆分

```
src/
├─ features/
│  └─ offline-map/                    ← 新增功能模块
│     ├─ types.ts                      公共类型
│     ├─ tileCache.ts                  IndexedDB 封装（get/put/delete/estimate）
│     ├─ tileDownload.ts               批量下载引擎（瓦片枚举 + 并发池 + 断点续传）
│     ├─ OfflineMapContext.tsx         全局状态（下载任务列表、进度、错误）
│     ├─ useOfflineMap.ts             消费 Context 的 hook
│     └─ components/
│        ├─ OfflineMapDialog.tsx       主弹窗（两 Tab）
│        ├─ DownloadTab.tsx            Tab1：下载离线地图
│        ├─ LocalTab.tsx               Tab2：本地缓存管理
│        └─ DownloadProgressBar.tsx    地图区悬浮进度条
│
├─ components/
│  └─ MapLibreContainer/
│     └─ MapLibreContainer.tsx         ← 改造：transformRequest 接入 tileCache
│
├─ pages/
│  └─ HomePage/
│     └─ HomePage.tsx                  ← 改造：挂载 SystemConfigButton + Dialog + ProgressBar
```

### 5.1 数据模型（types.ts）

```ts
/** 缓存的单个瓦片记录 */
interface TileCacheRecord {
  /** 主键：`${sourceId}/${z}/${x}/${y}` */
  key: string
  /** 来源标识（dark / satellite / glyphs / style） */
  sourceId: string
  z: number
  x: number
  y: number
  /** 瓦片二进制 */
  blob: Blob
  /** 内容类型：image/png | application/vnd.mapbox-vector-tile | ... */
  contentType: string
  /** 入库时间戳 */
  cachedAt: number
}

/** 预下载任务 */
interface DownloadTask {
  id: string
  basemap: 'dark' | 'satellite'
  /** 区域名称（预设名或"自定义区域"） */
  regionName: string
  /** 经纬度边界 */
  bbox: { west: number; east: number; south: number; north: number }
  minZoom: number
  maxZoom: number
  /** 状态机 */
  status: 'pending' | 'downloading' | 'paused' | 'completed' | 'failed'
  totalTiles: number
  completedTiles: number
  failedTiles: number
  bytesDownloaded: number
  error?: string
  createdAt: number
  updatedAt: number
}
```

### 5.2 IndexedDB 封装（tileCache.ts）

```ts
import { openDB, type IDBPDatabase } from 'idb'

const DB_NAME = 'gcs-tile-cache'
const DB_VERSION = 1
const STORE_TILES = 'tiles'
const STORE_TASKS = 'tasks'

let dbPromise: Promise<IDBPDatabase> | null = null
function getDB() {
  if (!dbPromise) {
    dbPromise = openDB(DB_NAME, DB_VERSION, {
      upgrade(db) {
        db.createObjectStore(STORE_TILES, { keyPath: 'key' })
        db.createObjectStore(STORE_TASKS, { keyPath: 'id' })
      },
    })
  }
  return dbPromise
}

export async function getTile(key: string): Promise<TileCacheRecord | undefined> {
  return (await getDB()).get(STORE_TILES, key)
}

export async function putTile(rec: TileCacheRecord): Promise<void> {
  await (await getDB()).put(STORE_TILES, rec)
}

export async function deleteTilesBySource(sourceId: string): Promise<void> {
  const db = await getDB()
  const tx = db.transaction(STORE_TILES, 'readwrite')
  let cursor = await tx.store.openCursor()
  while (cursor) {
    if (cursor.value.sourceId === sourceId) await cursor.delete()
    cursor = await cursor.continue()
  }
  await tx.done
}

export async function getCacheEstimate(): Promise<{ usage: number; quota: number }> {
  if (!navigator.storage?.estimate) return { usage: 0, quota: 0 }
  const { usage = 0, quota = 0 } = await navigator.storage.estimate()
  return { usage, quota }
}
```

### 5.3 下载引擎（tileDownload.ts）

核心算法：

1. **瓦片枚举**：由 bbox + zoom 范围，计算每层级行列号范围（标准 XYZ 瓦片公式）；
2. **并发池**：Promise 池控制并发数（默认 6），避免压垮服务端；
3. **断点续传**：下载前查询 IndexedDB 已有瓦片，跳过已缓存块；
4. **可中断**：暴露 `AbortController`，取消时已完成块自动入库。

```ts
/** 计算某 zoom 层级下，经纬度 → 瓦片行列号 */
function lonLatToTile(z: number, lon: number, lat: number) {
  const n = 2 ** z
  const x = Math.floor(((lon + 180) / 360) * n)
  const y = Math.floor(
    ((1 - Math.asinh(Math.tan((lat * Math.PI) / 180)) / Math.PI) / 2) * n,
  )
  return { x, y }
}

/** 枚举 bbox × zoom 范围内的所有瓦片坐标 */
export function enumerateTiles(
  bbox: DownloadTask['bbox'],
  minZoom: number,
  maxZoom: number,
): { z: number; x: number; y: number }[] {
  const tiles: { z: number; x: number; y: number }[] = []
  for (let z = minZoom; z <= maxZoom; z++) {
    const tl = lonLatToTile(z, bbox.west, bbox.north)
    const br = lonLatToTile(z, bbox.east, bbox.south)
    for (let x = tl.x; x <= br.x; x++) {
      for (let y = tl.y; y <= br.y; y++) {
        tiles.push({ z, x, y })
      }
    }
  }
  return tiles
}

/** 批量下载（支持并发、断点续传、中断） */
export async function downloadTask(
  task: DownloadTask,
  onProgress: (updated: DownloadTask) => void,
  signal: AbortSignal,
): Promise<void> {
  const allTiles = enumerateTiles(task.bbox, task.minZoom, task.maxZoom)
  // 断点续传：过滤已缓存
  const pending = []
  for (const t of allTiles) {
    const key = `${task.basemap}/${t.z}/${t.x}/${t.y}`
    if (!(await getTile(key))) pending.push({ ...t, key })
  }

  // 并发池（简化版，实际可用 p-limit）
  const CONCURRENCY = 6
  let index = 0
  async function worker() {
    while (index < pending.length) {
      if (signal.aborted) return
      const cur = pending[index++]
      try {
        const url = buildTileUrl(task.basemap, cur)
        const resp = await fetch(url, { signal })
        if (!resp.ok) throw new Error(`HTTP ${resp.status}`)
        await putTile({
          key: cur.key,
          sourceId: task.basemap,
          z: cur.z, x: cur.x, y: cur.y,
          blob: await resp.blob(),
          contentType: resp.headers.get('content-type') ?? 'application/octet-stream',
          cachedAt: Date.now(),
        })
        task.completedTiles++
        task.bytesDownloaded += resp.headers.get('content-length')
          ? Number(resp.headers.get('content-length'))
          : 0
        onProgress({ ...task })
      } catch (err) {
        if (signal.aborted) return
        task.failedTiles++
        onProgress({ ...task })
      }
    }
  }
  await Promise.all(Array.from({ length: CONCURRENCY }, worker))

  if (!signal.aborted && task.failedTiles === 0) {
    task.status = 'completed'
  } else if (signal.aborted) {
    task.status = 'paused'
  } else {
    task.status = 'failed'
    task.error = `${task.failedTiles} 块下载失败`
  }
  onProgress({ ...task })
}
```

### 5.4 MapLibreContainer 改造（严格离线，已落地）

`transformRequest` 将 tileserver 注入的绝对 origin 重写为 `/tiles` 同源代理路径，并进一步包装为 `gcs-cache://<path>` 自定义协议（`tileProtocol.ts`）。**严格离线**：协议层只查 IndexedDB，命中返回、未命中灰显，绝不在线回源。

```ts
// MapLibreContainer.tsx —— transformRequest 仅做 URL 重写 + 协议包装，不抓取
function createTileserverTransformRequest(useCacheProtocol = false) {
  return (url: string) => {
    if (url.startsWith(TILESERVER_ORIGIN)) {
      const proxyPath = url.replace(TILESERVER_ORIGIN, '/tiles')
      // 路由到 gcs-cache 自定义协议：缓存命中 / 未命中灰显（严格离线，不回源）
      if (useCacheProtocol) return { url: `gcs-cache://${proxyPath}` }
      return { url: proxyPath }
    }
    return { url }
  }
}
```

```ts
// tileProtocol.ts —— gcs-cache 协议处理器（严格离线核心不变式）
addProtocol(CACHE_PROTOCOL, async (params) => {
  const path = normalizeUrlToPath(params.url)
  // 1. 命中缓存 → 零网络返回（唯一数据来源）
  const cached = await getTile(path)
  if (cached) return { data: await cached.blob.arrayBuffer() }
  // 2. 未命中 → 严格离线，绝不回源任何 tileserver / 在线服务：
  //    栅格瓦片 → 灰色 PNG 占位；矢量瓦片 / 字体 / sprite → 抛错（露出暗色背景）
  const m = matchTilePath(path)
  if (m && m.ext !== 'pbf') {
    const gray = await getGrayPngTile()
    if (gray) return { data: gray }
  }
  throw new Error('offline: tile not cached')
})
```

> **关键约束（废弃懒加载/在线兜底）**：旧版设计曾考虑"未命中 + 在线 → `fetchAndCache` 走网络写回缓存（懒加载）"，**该方案已废弃**。严格离线要求：无论 `navigator.onLine` 为何值，缓存未命中一律灰显，保证渲染完全离线、无任何 Esri / 在线兜底。该不变式由 `tileProtocol.test.ts` 锁定（"`navigator.onLine === true` 时未命中也不回源"）。
>
> **实现备注**：MapLibre 的 `transformRequest` 同步返回 `{ url }`，不直接支持返回 Blob，故通过 `maplibre-gl` 的 `addProtocol` API 注册 `gcs-cache://` 自定义协议在 worker 中拦截请求并返回缓存的 ArrayBuffer。

---

## 6. 关键细节

### 6.1 灰显瓦片实现

缺失块灰显的两种方案：

| 方案 | 实现 | 优缺点 |
|------|------|--------|
| **A. 灰色占位瓦片** | `transformRequest` 缓存未命中且离线时，返回一个 256×256 灰色 PNG/SVG Blob | 简单直接；但每个缺失块都需返回，有轻微开销 |
| **B. 背景层灰显** | 地图容器背景色设为灰色棋盘，瓦片缺失处自然透出背景 | 更轻量；但无法区分"加载中"与"缺失" |

**推荐 A**：视觉明确，与"已下载块正常"形成对比。灰色 Blob 可预生成常量，无网络开销。

### 6.2 瓦片数与空间预估

```ts
// 瓦片数计算（已在 enumerateTiles 实现）
const count = enumerateTiles(bbox, minZoom, maxZoom).length

// 空间预估：按底图类型取平均单块大小
const AVG_TILE_SIZE = { dark: 20_000, satellite: 45_000 }  // bytes
const estimatedBytes = count * AVG_TILE_SIZE[basemap]
```

### 6.3 断点续传

- 下载前调用 `getTile(key)` 判断已存在则跳过；
- 中断/失败时已完成块已在 IndexedDB，下次重试自动续传；
- `DownloadTask` 状态持久化到 IndexedDB `tasks` store，刷新页面后可恢复进度。

### 6.4 配额与持久化

- 首次进入"下载离线地图"Tab 时调用 `navigator.storage.persist()` 请求持久化，避免浏览器在低空间时自动清理；
- 大批量下载前检查 `navigator.storage.estimate()`，超 80% 配额时警告。

---

## 7. 状态机

### 7.1 DownloadTask 状态流转

```
pending ──start──▶ downloading ──complete──▶ completed
                       │
                       ├──pause/abort──▶ paused ──resume──▶ downloading
                       │
                       └──error──▶ failed ──retry──▶ downloading
```

### 7.2 地图渲染状态（扩展 MapLibreContainer status）

现有 `status: 'loading' | 'success' | 'error'`（`MapLibreContainer.tsx`），扩展为：

```ts
type MapStatus = 'loading' | 'success' | 'error' | 'partial'
```

- `partial`：实时加载失败但命中 IndexedDB style 缓存回退，瓦片层可能部分缺失（gcs-cache 协议对未命中瓦片返回灰色占位），不打断用户；
- `error`：style.json 加载失败且无本地缓存（如本地 tileserver-gl 未启动）。

> 严格离线：**不读取 `navigator.onLine`，无 `offline` 状态**。早期「断网 + 零缓存 → 全屏灰显 + 下载提示」分支已废弃（`OfflineMapPlaceholder` 组件已移除）；瓦片是否灰显完全由 IndexedDB 缓存命中决定，与联网与否无关。

---

## 8. 备选方案：Service Worker（未采用）

`transformRequest + gcs-cache 自定义协议` 方案已落地，`maplibre-gl` `addProtocol` API 注册成功，无需降级。

> **不采用 SW 的关键原因（严格离线）**：SW 方案天然倾向"未命中回源"（命中则 `new Response(blob)`，未命中则 `fetch` 回源并写缓存），这与"严格离线、绝不在线兜底"的核心约束相悖；且 SW 生命周期复杂、调试困难、需处理版本更新与缓存失效。当前 `gcs-cache` 协议在未命中时直接灰显、零网络，已满足需求，故 SW 方案存档不实现。

---

## 9. 落地计划

| 阶段 | 工作项 | 预估 |
|------|--------|------|
| P0 | IndexedDB 封装（`tileCache.ts`）+ 类型定义 | 0.5 天 |
| P0 | MapLibreContainer `transformRequest` + `gcs-cache` 协议接入缓存（严格离线模式） | 1 天 |
| P0 | 灰显瓦片 + `offline`/`partial` 状态覆盖层 | 0.5 天 |
| P1 | `OfflineMapDialog` 骨架（两 Tab）+ `SystemConfigButton` 入口 | 0.5 天 |
| P1 | DownloadTab：区域/层级选择 + 预估 + 下载引擎 + 进度条 | 2 天 |
| P1 | LocalTab：缓存列表 + 删除 + 配额展示 | 1 天 |
| P2 | 断点续传 + 任务持久化（刷新恢复） | 1 天 |
| P2 | 成功/失败弹框 + 错误文案完善 | 0.5 天 |
| P2 | 联调测试（断网模拟、部分缺失、配额边界） | 1 天 |

**总计**：约 8 天。建议按 P0 → P1 → P2 分批交付，P0 完成后即具备"严格离线缓存 + 灰显"基础能力（已落地）。

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| MapLibre worker 协议注册复杂 | P0 阻塞 | 预研 `maplibre-gl` `addProtocol` API；备选 SW 方案 |
| IndexedDB 大容量写入性能 | 下载慢 | 批量事务（每 100 块一次 commit）；`idb` 库已优化 |
| 浏览器存储配额限制（默认 ~2GB） | 下载中断 | `persist()` + 预检 `estimate()` + 超限警告 |
| satellite 栅格瓦片体积大（单块 ~45KB） | 全层级下载耗时数小时 | 限制 satellite 最大 zoom ≤ 16；推荐仅 dark 矢量全量下载 |
| 浏览器隐私模式 IndexedDB 受限 | 功能不可用 | 检测并提示"请退出隐私模式" |

---

## 11. 与现有文档的关系

- `docs/离线部署指南.md`：**服务端方案**（tileserver-gl + mbtiles），仍是数据来源，保留不变；
- 本文档：**客户端方案**（浏览器 IndexedDB 缓存），在服务端方案之上新增一层，使前端具备离线自治能力；
- 两者关系：tileserver-gl 提供"首次下载源"，IndexedDB 提供"运行时缓存层"，断网后前端完全依赖后者。